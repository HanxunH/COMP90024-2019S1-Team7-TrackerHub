version: '3'
services:
  backend:
    container_name: spider
    build: {{remote_working_directory}}/scraper/GetResearchData
    volumes:
      - {{remote_working_directory}}/scraper:/workspace
    restart: always
