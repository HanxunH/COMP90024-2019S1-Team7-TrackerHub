- hosts: all
  sudo: yes
  vars_files:
    - host_vars/remote_vars.yaml
  gather_facts: true
  roles:
    - role: install-environments
    - role: network-penetration

- hosts: all
  vars_files:
    - host_vars/remote_vars.yaml
  gather_facts: true
  roles:
    - role: git-clone-sourcefile


# - hosts: WebServer
#   vars_files:
#     - host_vars/vars.yaml
#   gather_facts: true
#   roles:
#     - role: config-webserver
#     - role: deploy-applications